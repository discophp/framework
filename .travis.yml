language: php
php:
    - 5.6
    - 5.5
    - 5.4

before_script: 
    - mysql -e 'CREATE DATABASE test_db;' -uroot
    - sudo apt-get update -qq
    - git clone https://github.com/discophp/project.git
    - cd project
    - git branch dev-master
    - composer install

script: phpunit
